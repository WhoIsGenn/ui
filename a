task.spawn(function()
    task.wait(2)
    pcall(function()
        if keypress then
            keypress(0x46)  -- F key
            task.delay(0.05, function()
                pcall(function() keyrelease(0x46) end)
            end)
            print("[INVIS HUB] Keypress F sent to capture remote.")
        end
    end)
end)

task.wait(1.5)

if next(_reverted) ~= nil then
    print("[INVIS HUB] Remote parry captured successfully!")
else
    warn("[INVIS HUB] Remote belum ke-capture. Mungkin butuh parry manual 1x.")
end
